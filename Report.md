# バス運行状況確認システム：運行・通信仕様レポート

本システムにおけるバス位置情報の更新頻度、およびデータ通信量・バッテリー消費に関する技術仕様と目安をまとめました。

## 1. 位置情報の更新頻度

データ通信量を最適化しつつ、利用者がストレスなく運行状況を確認できるよう、以下の更新間隔に設定されています。

| 方式 | 頻度 | 用途 | 説明 |
| :--- | :--- | :--- | :--- |
| **WebRTC (リアルタイム)** | **3秒ごと** | スムーズな位置表示 | 走行中のバスの動きを滑らかに描写し、現在の時速をリアルタイムで表示するために使用されます。 |
| **Firestore (バックアップ)** | **5分ごと** | 永続的な状態管理 | 接続の安定化や、WebRTCが繋がっていない利用者へのバックアップ、および**6分以上**の無通信を「時間外」と判定するために使用されます。 |
| **OSRM (ルート再計算)** | **3分ごと** | 道路沿いのルート描画 | バスが現在の道路状況に合わせた正確なルート（紫色の線）を維持するために、定期的に経路を最適化します。 |

---

## 2. データ通信量の目安

以下の数値は、1時間あたりの通信量の予測値です（利用環境や地図の操作頻度により変動します）。

### 🚌 送信側（バスの運転手用アプリ）
*   **推定通信量: 約 8MB 〜 15MB / 1時間**
*   **内訳:**
    *   30名程度の保護者が同時に閲覧している場合を想定した WebRTC 通信（3秒間隔に最適化済み）
    *   5分ごとのサーバー（Firestore）同期
    *   地図データの初期読み込み

### 📱 受信側（保護者用アプリ）
*   **推定通信量: 約 4MB 〜 8MB / 1時間**
*   **内訳:**
    *   リアルタイムの位置情報受信（WebRTC / Firestore）
    *   地図タイル（OpenStreetMap）の読み込み（移動量に応じて変動）
    *   3分ごとの道路沿いルート（OSRM）取得

> **💡 通信量節約のポイント:**
> アプリをバックグラウンドにした状態や、画面を閉じている間は WebRTC の通信が自動的に抑制されるため、実際の消費量は上記よりも大幅に少なくなる可能性があります。

---

## 3. バッテリー消費と最適化

### 送信側（バス側）の留意点
*   **高精度GPSの利用:** 正確な位置を特定するため、端末の GPS センサーをフル稼働させます。
*   **スリープ防止 (Wake Lock):** 送信中に画面が消灯して通信が途切れるのを防ぐため、スリープ防止機能を有効にしています。
*   **推奨事項:** 走行中はシガーソケット等からの**常時給電**を強く推奨します。

### 受信側（保護者側）の留意点
*   **画面表示:** 地図の描画とバスアイコンの移動は CPU/GPU を使用するため、ブラウザを長時間開いたままにするとバッテリーを消費します。
*   **省電力動作:** バスの位置が 6 分間更新されない場合、システムは自動的に低負荷モード（更新の減衰表示）に切り替わります。

---

## 4. セキュリティと通信効率の工夫

*   **E2EE暗号化:** すべての位置データは端末側で AES-256 暗号化されてから送信されるため、サーバー上でも内容は解読できません。
*   **P2P通信:** 位置情報の大部分をサーバーを介さない P2P（WebRTC）でやり取りすることで、サーバー遅延を最小限に抑えています。
